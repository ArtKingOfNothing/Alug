version: '3.8'

services:
  mysql-alug:
    image: mysql:8.0
    container_name: alug-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: alug_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: